import { StreamerTagFilterPipe } from './streamer-tag-filter.pipe';

describe('StreamerTagFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new StreamerTagFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
