import { StreamerSortPipe } from './streamer-sort.pipe';

describe('StreamerSortPipe', () => {
  it('create an instance', () => {
    const pipe = new StreamerSortPipe();
    expect(pipe).toBeTruthy();
  });
});
